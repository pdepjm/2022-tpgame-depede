import wollok.game.*
import personaje.*
import zombie.*

program juego {
	game.width(30)
 	game.height(19)
 	game.cellSize(50)
 	game.addVisualCharacter(personaje)
 	game.addVisual(zombie)
 	zombie.acercate(personaje)
    game.start()
  }
