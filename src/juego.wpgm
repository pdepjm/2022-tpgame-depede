import wollok.game.*
import personaje.*
import zombie.*

program juego {
	game.width(30)
 	game.height(19)
 	game.cellSize(50)
 	game.addVisualCharacter(personaje)
 	game.addVisual(zombie)
    game.onTick(200, "movimiento", { zombie.moverse() })
 	
    game.start()
    
  }
